import Peer from 'peerjs';

//============================================================================= PeerConnector
class PeerConnector{
	constructor(){
		// this.peer = new Peer('pick-an-id', {key: 'myapikey'}); 
		let host = 'localhost';	//'192.168.56.1'
		let port = 3000;
		// this.peer = new Peer('sm_control', {host, port});


	}

}

export default PeerConnector;
